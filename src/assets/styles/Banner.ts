import styled from "styled-components";

type SrcProps = {
    src: string
}

export const Banner = styled.div<SrcProps>`
    color: white;
    background: url();
    background-size: cover;
    width: 100%;
    height: 10vh;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    h1{

    }
    h2{
        
    }
`